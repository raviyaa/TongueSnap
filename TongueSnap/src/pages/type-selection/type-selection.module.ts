import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { TypeSelectionPage } from './type-selection';

@NgModule({
  declarations: [
    TypeSelectionPage,
  ],
  imports: [
    IonicPageModule.forChild(TypeSelectionPage),
  ],
})
export class TypeSelectionPageModule {}
